name: Create/update tag
on:
  push:
    branch: "master"
jobs:
  create-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: rickstaa/action-create-tag@v1
        with:
          tag: ${{ $(date '+%Y-%m-%d') }}
          message: "$tagname release"
          
